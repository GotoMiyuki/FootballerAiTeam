# FootballAI Career Agent — runtime dependencies
# Tested with Python 3.10+ on Windows (CPU-only PyTorch).

--extra-index-url https://download.pytorch.org/whl/cpu

# LLM orchestration
langchain-openai==1.3.5
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langgraph==1.2.9
python-dotenv==1.2.2

# Local RAG: Chroma vector store, PDF loading, BGE embedding and reranking
langchain-chroma==1.1.0
chromadb==1.5.9
pypdf==6.16.2
FlagEmbedding==1.4.2
torch==2.13.0+cpu

# Optional: live web search (the app falls back to local RAG when omitted)
tavily-python==0.7.26
